name: Timeline
description: >
  Conversation package type.
  Tamper-evident sequence of entries (hash-linked chain) for a conversation.
  Each new entry references the preceding entry by identifier to preserve order.
timelineId:
  type: Text
  description: Stable identifier of this timeline (application-defined).
